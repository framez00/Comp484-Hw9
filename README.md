1. A list of the built-in objects and methods you used

- Date Object:
    - getMonth()
    - getDate()
    - getFullYear()

- Number Object:
    - Number()
    - Number.isNaN()
    - Number.isInteger()
    - toFixed

- Document Object
    - getElementById()
    - innerHTML
    - textContent

2. Your **GitHub Pages link**
- https://framez00.github.io/Comp484-Hw9/
3. Path to a screenshot file of the finished webpage
- screenshot.png
4. A short reflection of **4-5 sentences** that answers:
   - What part was easiest?
        - The easiest part was the date display section because it was just simple coding and extracting the inforation (date, month, year)
   - What part was hardest?
        - The hardest part were the number conversions and putting together the output sentences. I wasn't quite sure what the best and most efficient way was to output the results.
   - What did you learn about the `Date` object?
        - I learned that you can just simply use the Date object and extract the individual information by just using the methods listed in above (in 1).
   - What did you learn about the `Number` object?
        - I learned that the Number object is makes it easy to convert numbers and gain more insights on them (such as finding out if they are NaN or Integers).
   - What did you learn about displaying results in the browser?
        - I learned that displaying results can simply be done by selecting the HTML elements and update those using JavaScript.
